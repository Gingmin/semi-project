package com.greedy.semi.admin.controller;

public class sad {

	
	public static void main(String[] args) {
		
	String name = "";
	int ptAmount = -1;
	int no = 0;
	String phone = "";
	System.out.println(ptAmount == 0 && !(no < 0 && name.length() < 0 && phone.length() < 0));
	
	/*                      name.length() > 0 && !(no < 0 && phone.length() < 0) && ptAmount == -1
			
		no > 0 && !(name.length() < 0 && phone.length() < 0) && ptAmount == -1
			
		phone.length() > 0 && !(no < 0 && name.length() < 0) && ptAmount == -1
			
		ptAmount > 0 && !(no < 0 && name.length() < 0 && phone.length() < 0)
			
		ptAmount == 0 && !(no < 0 && name.length() < 0 && phone.length() < 0)
			
		name.length() > 0 && no > 0 && !(phone.length() < 0) && ptAmount == -1
			
		name.length() > 0 && phone.length() > 0 && !(no < 0) && ptAmount == -1
			
		no > 0 && phone.length() > 0 && !(name.length() < 0) && ptAmount == -1
			
		name.length() > 0 && no > 0 && phone.length() > 0 && ptAmount == -1
			
		

		name.length() > 0 && ptAmount > 0 && !(phone.length() < 0 && no < 0)										//두번쨰
			
		no > 0 && ptAmount > 0 && !(phone.length() < 0 && name.length() < 0)
			
		phone.length() > 0 && ptAmount > 0 && !(name.length() < 0 && no < 0)
			
		name.length() > 0 && no > 0 && ptAmount > 0 && !(phone.length() < 0)
			
		name.length() > 0 && phone.length() > 0 && ptAmount > 0 && !(no < 0)
			
		no > 0 && phone.length() > 0 && ptAmount > 0 && !(name.length() < 0)
			
		name.length() > 0 && no > 0 && phone.length() > 0 && ptAmount > 0
			
		 

		name.length() > 0 && ptAmount == 0 && !(no < 0 && phone.length() < 0)										//세 번째
			
		no > 0 && ptAmount == 0 && !(name.length() < 0 && phone.length() < 0)
	
		phone.length() > 0 && ptAmount == 0 && !(name.length() < 0 && no < 0)
			
		name.length() > 0 && no > 0 && ptAmount == 0 && !(phone.length() < 0)
		
		name.length() > 0 && phone.length() > 0 && ptAmount == 0 && !(no < 0)
			
		no > 0 && phone.length() > 0 && ptAmount == 0 && !(name.length() < 0)
			
		name.length() > 0 && no > 0 && phone.length() > 0 && ptAmount == 0 */
	
	}
}
